defvar(tester func([expr]
	echo("================================\n")
	raw/echo(expr)
	echo("\n↑↑ Raw ↑↑ ------- ↓↓ Compiled ↓↓\n")
	raw/echo(compile(expr))
	echo()
))

tester(raw([1 #t &test "test" {id "test"}]))

tester(raw(+(1 2)))

tester(raw(
	=>(4
		+(1)
		*(=>(1
			+(2))))
))
