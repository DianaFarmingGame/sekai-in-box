(block
	(defvar CraftTable {})
	(defvar table (csv/map-let (str/concat [*config_dir* "craft_table.csv"])
		[ID 需求物品 合成物品 描述]
		{
			ID (keyword ID)
			need ( => 需求物品
				(string/split ";")
				(array/filter (func [l] (!= l "")))
				(array/map (
					func [l] ( => l
						(string/split ":")
						(array/map string/trim)
					)
				))
			)

			craft (
				string/split (string/trim 合成物品) ":"
			)

			desc 描述
		}
	))
	(echo table)
)