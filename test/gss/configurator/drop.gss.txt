; {drops: [{itemID: stringName, min_count: int, max_count: int, probability: [float]}, ...]}}

(block
	(csv/map-let (str/concat [*config_dir* "drop.csv"])
		[破坏方块 掉落 数量 概率] 
		(dbs/pushp ["掉落" 破坏方块 "drops" {
			itemID 		掉落
			min_count	(num (@ (string/split 数量 "-") 0))
			max_count	(num (@ (string/split 数量 "-") 1))
			probability (array/map (string/split 概率 " ") num)
		}])
	)
)