defunc (clip_portal [x y]
	[rect2(-0.5 -1.4 1 1.5) rect2(*(x 32) *(y 48) 32 48)]
)
defunc (anim_portal [y]
	array/unfold(4 fn ([x] clip_portal (x y)))
)

define (load ("../block_static_ext.gd")
	ref 2300
	id &环境/被动/传送门
	props {
		need_route #f
		routable #f
		need_collision #f
		collisible #f
		asserts {
			main load ("portals.png")
		}
		cur_draw &main
		draw_flip_h #t
		draw_data {
			main [&fixed &main 0.6 anim_portal(0)]
		}
	}
)
