; 垂直地板
define (load ("../block_static_ext.gd")
	ref 302
	props {
		need_collision  #f
		collisible      #f
		asserts {
			main load ("../房间.png")
		}
		cur_draw &floor
		draw_data {
			floor       [&static &main clip_ground(4 3)]
		}
	}
)

; 水平地板
define (load ("../block_static_ext.gd")
	ref 303
	props {
		need_collision  #f
		collisible      #f
		asserts {
			main load ("../房间.png")
		}
		cur_draw &floor
		draw_data {
			floor       [&static &main clip_ground(3 3)]
		}
	}
)

